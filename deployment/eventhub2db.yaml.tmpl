apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventhub2db
  namespace: TENANT
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eventhub2db
  template:
    metadata:
      labels:
        app: eventhub2db
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
        - name: eventhub2db
          image: EVENTHUB2DB_IMAGE
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          env:
            - name: EVENTHUB_CONNECTIONSTRING
              valueFrom:
                secretKeyRef:
                  name: connectionstring
                  key: eventhubConn
        - name: timescaledb
          image: timescale/timescaledb:2.1.0-pg13
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              value: "password"
        - name: grafana
          image: grafana/grafana:7.4.3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: ci4rail-harbor-secret
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: TENANT
spec:
  type: ClusterIP
  ports:
    - port: 3000
  selector:
    app: grafana
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: TENANT
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-production
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  tls:
    - hosts:
        - grafana.ci4rail.com
      secretName: grafana-tls-secret
  rules:
    - host: grafana.ci4rail.com
      http:
        paths:
          - path: /TENANT
            pathType: Prefix
            backend:
              service:
                name: grafana-service
                port:
                  number: 3000
---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: grafana-tls-secret
  namespace: TENANT
spec:
  secretName: grafana-tls-secret
  dnsNames:
    - grafana.ci4rail.com
  commonName: grafana.ci4rail.com
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
